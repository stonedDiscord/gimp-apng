name: Ubuntu Build

on:
  push:
    branches: [ "list" ]
  pull_request:
    branches: [ "list" ]

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v3
    - name: install dependencies
      run: sudo apt install gettext autoconf automake intltool libtool
    - name: configure
      run: |
        ./autogen.sh
        ./configure
    - name: make
      run: |
        make
    - name: make install
      run: make install
