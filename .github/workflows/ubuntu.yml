name: Ubuntu Build

on:
  push:
    branches: [ "list" ]
  pull_request:
    branches: [ "list" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: install dependencies
      run: sudo apt install autoconf2.69
    - name: configure
      run: |
        autoconf-2.69
        ./configure
    - name: make
      run: |
        automake
        make
    - name: make install
      run: make install
